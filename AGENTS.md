# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v55.0.0/ before writing any code.

(This project was downgraded from SDK 56 to SDK 55 so it opens in the App Store / Play Store Expo Go, which doesn't support SDK 56 yet.)
